name: Release

on:
  push:
    tags:
      - "v*.*.*"

permissions:
  contents: write

env:
  CARGO_TERM_COLOR: always
  GSR_SKIP_FFMPEG_DOWNLOAD: "1"
  CMAKE_BUILD_PARALLEL_LEVEL: "2"
  CARGO_BUILD_JOBS: "2"
  JOBS: "2"

jobs:
  crate:
    name: package crate
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - uses: actions/cache@v4
        with:
          path: third_party/ncnn-src
          key: ncnn-20260526-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('build.rs', 'scripts/build_ncnn_static.sh') }}
      - name: Install system ffmpeg and CMake
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends ffmpeg g++ cmake
      - name: Check tag matches Cargo.toml version
        run: |
          set -euo pipefail
          tag="${GITHUB_REF_NAME}"
          expect="${tag#v}"
          got="$(sed -n 's/^version = "\([^"]*\)"/\1/p' Cargo.toml | head -n1)"
          if [ "$got" != "$expect" ]; then
            echo "Cargo.toml version '$got' does not match tag '$tag'" >&2
            exit 1
          fi
          echo "version $got matches $tag"
          echo "VERSION=$got" >> "$GITHUB_ENV"
      - name: Build static ncnn (live cmake log)
        shell: bash
        run: |
          export TARGET="$(rustc --print host-tuple)"
          echo "TARGET=$TARGET"
          bash scripts/build_ncnn_static.sh
      - name: Test (default features)
        run: cargo test
      - name: Test video + FDnCNN
        run: cargo test --features video-fdncnn,system-ffmpeg
      - name: Package crate
        run: cargo package
      - name: Checksums
        run: |
          set -euo pipefail
          crate="target/package/gemini-sparkle-remove-${VERSION}.crate"
          sha256sum "$crate" | tee SHA256SUMS.txt
      - uses: actions/upload-artifact@v4
        with:
          name: crate
          path: |
            target/package/gemini-sparkle-remove-*.crate
            SHA256SUMS.txt

  bin:
    name: bin (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    timeout-minutes: 90
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            artifact: gemini-sparkle-remove-linux-x86_64
          - os: macos-latest
            artifact: gemini-sparkle-remove-macos-aarch64
          - os: windows-latest
            artifact: gemini-sparkle-remove-windows-x86_64.exe
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - uses: actions/cache@v4
        with:
          path: third_party/ncnn-src
          key: ncnn-20260526-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('build.rs', 'scripts/build_ncnn_static.sh') }}
      - name: Install Linux deps
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends ffmpeg g++ cmake
      - name: Install macOS ffmpeg
        if: runner.os == 'macOS'
        run: brew install ffmpeg
      - name: Install Windows ffmpeg
        if: runner.os == 'Windows'
        run: choco install ffmpeg -y --no-progress
      - name: Build static ncnn (live cmake log)
        shell: bash
        run: |
          export TARGET="$(rustc --print host-tuple)"
          echo "TARGET=$TARGET"
          bash scripts/build_ncnn_static.sh
      - name: Build CLI
        run: cargo build --release --features cli --bin gemini-sparkle-remove
      - name: Stage binary
        shell: bash
        run: |
          if [ -f target/release/gemini-sparkle-remove.exe ]; then
            cp target/release/gemini-sparkle-remove.exe "${{ matrix.artifact }}"
          else
            cp target/release/gemini-sparkle-remove "${{ matrix.artifact }}"
          fi
      - uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: ${{ matrix.artifact }}

  github-release:
    name: GitHub Release
    needs: [crate, bin]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/download-artifact@v4
        with:
          path: dist
          merge-multiple: true
      - name: Create GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          ls -la dist
          gh release create "$GITHUB_REF_NAME" \
            --title "$GITHUB_REF_NAME" \
            --generate-notes \
            dist/*
